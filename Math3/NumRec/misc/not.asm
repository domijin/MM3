DATA	SEGMENT PUBLIC 'DATA'
DATA	ENDS

DGROUP	GROUP DATA

CODE	SEGMENT 'CODE'
	ASSUME CS:CODE,DS:DGROUP,SS:DGROUP

	PUBLIC NOT
NOT	PROC FAR
	PUSH BP
	MOV BP,SP
	LES BX,DWORD PTR [BP+6]
	MOV AX,ES:[BX]
	MOV DX,ES:[BX]+2
	NOT AX
	NOT DX
	MOV SP,BP
	POP BP
	RET 04H
	NOT ENDP
CODE	ENDS
	END
